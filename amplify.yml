version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static household-console (HTML/CSS/JS)"
  artifacts:
    baseDirectory: household-console
    files:
      - "**/*"
  cache:
    paths: []
